name: Final surface polish

on:
  push:
    paths:
      - '.github/workflows/final-surface-polish.yml'

permissions:
  contents: write

jobs:
  polish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply polish
        run: python3 .github/scripts/apply_final_surface_polish.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet && exit 0
          git commit -m "Polish final surfaces and set advisory metric to 2+1"
          git push
